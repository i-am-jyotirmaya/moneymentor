# Public values bundled into the app; never put secrets here.
# Use the API origin (without /api). A phone must be able to reach this HTTPS URL.
NEXT_PUBLIC_API_BASE_URL=https://api.spndrr.example
NEXT_PUBLIC_SUPPORT_EMAIL=support@spndrr.com
# Origin accepted when pasting an approved web signup link.
NEXT_PUBLIC_WEB_APP_URL=https://mvp.spndrr.com
